<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nsucpcadmin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NSUCPCAdmin">
        <activity android:name=".ui.activities.editprofile.EditProfileActivity"></activity>
        <activity android:name=".ui.activities.base.BaseActivity" />
        <activity
            android:name=".ui.activities.auth.ForgetPasswordActivity"
            android:theme="@style/Theme.NoActionBar" />
        <activity
            android:name=".ui.activities.auth.LoginActivity"
            android:theme="@style/Theme.NoActionBar" />
        <activity
            android:name=".ui.activities.auth.SignupActivity"
            android:theme="@style/Theme.NoActionBar" />
        <activity
            android:name=".ui.activities.splash.SplashActivity"
            android:theme="@style/Theme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>